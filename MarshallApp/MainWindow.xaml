<Window x:Class="MarshallApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarshallApp"
        mc:Ignorable="d" Height="800" Width="1200" ResizeMode="CanResizeWithGrip" WindowStyle="ToolWindow" Background="{x:Null}" Foreground="#FFBDBDBD" Title="Marshall">
    <Grid x:Name="RootGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="LeftCol" Width="200" MaxWidth="300"/>
            <ColumnDefinition Width="8"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="8"/>
            <ColumnDefinition x:Name="RightCol" Width="300" MaxWidth="600"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.ColumnSpan="5" Orientation="Horizontal" Background="#222">
            <Button x:Name="AddButton" Content="Menu" Style="{StaticResource FlatButtonStyle}" Click="AddButton_Click" Margin="5,0">
                <Button.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Add block" Click="AddBlock_Click"/>
                        <Separator/>
                        <MenuItem Header="Settings" Click="Settings_Click"/>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
        </StackPanel>

        <local:ScriptBrowserPanel x:Name="ScriptBrowser" Grid.Row="1" Grid.Column="0" 
                              ScriptSelected="ScriptBrowser_ScriptSelected"
                              Visibility="{Binding LeftPanelVisible, Converter={StaticResource BoolToVis}}"/>
        
        <GridSplitter Grid.Row="1" Grid.Column="1" Width="8" Background="#333" HorizontalAlignment="Stretch"/>

        <Grid x:Name="MStackPanel" Grid.Row="1" Grid.Column="2" Background="#111"/>

        <GridSplitter Grid.Row="1" Grid.Column="3" Width="8" Background="#333" HorizontalAlignment="Stretch"/>

        <local:CodeEditorPanel x:Name="CodeEditor" Grid.Row="1" Grid.Column="4"/>
    </Grid>
</Window>