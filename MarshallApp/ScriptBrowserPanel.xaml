<UserControl x:Class="MarshallApp.ScriptBrowserPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <UserControl.Resources>
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                SnapsToDevicePixels="True">

                            <ContentPresenter 
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="Transparent"/>
                            </Trigger>

                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="Transparent"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="{StaticResource PanelsBrush2}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
            <TextBlock Text="Browser" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="Bold" Margin="10" FontSize="14"/>
        </StackPanel>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource FlatButtonStyle}" Width="20" Height="20" Margin="3" Click="RefreshButton_Click">
                <Path 
                    Stretch="Uniform"
                    Stroke="#CCFFFFFF" 
                    StrokeThickness="3"
                    StrokeLineJoin="Round"
                    Data="M12 2A10 10 0 1 0 22 12H19 L23 8L27 12H24A12 12 0 1 1 12 0Z"/>
            </Button>
        </StackPanel>
        <ListBox x:Name="ScriptList"
                 Grid.Row="1"
                 Background="{StaticResource PanelsBrush}"
                 Foreground="{StaticResource PrimaryTextBrush}"
                 Margin="5"
                 HorizontalContentAlignment="Stretch"
                 Style="{StaticResource ModernListBoxStyle}"
                 ItemContainerStyle="{StaticResource ModernListBoxItemStyle}"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Hidden">
            <ListBox.Resources>
                <Style TargetType="ScrollViewer" BasedOn="{StaticResource DarkScrollViewerStyle}"/>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource DarkScrollBarVertical}"/>
            </ListBox.Resources>
        </ListBox>
    </Grid>
</UserControl>